const app = Vue.createApp({
    data() {
        return {
            product: {
                name: 'Socks',
                description: "Blue socks with VueJS logo"
            }
        }
    }
});